# Coverage.py configuration
# https://coverage.readthedocs.io/en/latest/config.html

[run]
source = app
omit =
    # Test files
    tests/*
    */tests/*
    
    # Virtual environment
    .venv/*
    */site-packages/*
    
    # A static lookup table with one trivial accessor (tested in test_food_mapping.py)
    app/services/food_mapping.py

[report]
# Show missing lines
show_missing = True

# Skip fully covered files in reports
skip_covered = True

# Precision for coverage percentages
precision = 2

# Exclude lines with pragma comments
exclude_lines =
    # Default exclusions
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
    @abc.abstractmethod

[html]
directory = htmlcov
